package com.appbytes.beautywallpaper.util

import android.view.View
import androidx.recyclerview.widget.RecyclerView
import com.appbytes.beautywallpaper.ui.main.home.ImageAdapter
import kotlinx.android.synthetic.main.item_loading.view.*

class GenericViewHolder
constructor(
    itemView: View
): RecyclerView.ViewHolder(itemView){

    /*companion object {
        fun test() {

        }
    }
    fun bind() {

    }

    fun changeInnerBottom(bottomState : Int) {
        if (bottomState == ImageAdapter.LOADING) {
            itemView.loading_progress_bar.visibility = View.VISIBLE
            itemView.loading_retry.visibility = View.GONE
        }
        else {
            itemView.loading_progress_bar.visibility = View.GONE
            itemView.loading_retry.visibility = View.VISIBLE
        }
    }*/


}